Decide whether source law A ({{ equation1 }}) implies target law B ({{ equation2 }}) over all magmas (binary op `*`).

CRITICAL BUDGET RULE: Keep reasoning under 400 words total. Your reply MUST end with `\boxed{TRUE}` or `\boxed{FALSE}` on its own line. If you are close to running out of space, stop reasoning IMMEDIATELY and emit the boxed verdict based on what you've decided so far. Never leave the reply without a boxed verdict.

## How to proceed

Check each step in order. Stop and output the verdict at the first decisive match.

### STEP 0 — Source-collapse lemmas (TRUE shortcuts for bare `x = (…)` laws)

Normalize A so the lone variable is on the LEFT and rename it `x`; rename others by first appearance → `y, z, w`. Then:
- A is `x = x*(y*(z*(x*y)))` or `x = x*((y*z)*(z*z))` → LEFT PROJECTION `p*q = p`. Evaluate B under left projection (each product collapses to its leftmost variable). Both sides of B same variable ⇒ TRUE, else FALSE.
- A is `x = (((y*z)*x)*z)*x` → RIGHT PROJECTION `p*q = q`. Both sides of B collapse to same variable ⇒ TRUE, else FALSE.

Also: if A's LHS is `x*y` and on the RHS every occurrence of `x` lies only in the right child of the top split (none in the left), apply CONSTANT-PRODUCT collapse (every non-variable product collapses to one symbol `c`). Both sides of B collapse to same symbol ⇒ TRUE, else FALSE.

### STEP 0B — Contradiction motifs (TRUE shortcuts for bare A)

Compute on the product side of A only: `rhsVars` (distinct vars), variable counts, `Lx` (leftmost is x?), `Rx` (rightmost is x?), `xTop` (at top split `U*V`: `left`/`right`/`both` for where x appears), `square` (has `u*u`?), `topShape` (`v-m` = var*product, `m-v` = product*var, `m-m` = both products).

Any of these ⇒ TRUE:
- C1: rhsVars=4, ¬Lx, ¬Rx
- C2: counts=3,1,1, ¬Lx, ¬Rx
- C3: ¬Lx, xTop=left, square, topShape=m-v
- C4: counts=2,1,1, ¬Lx, ¬Rx, xTop=right, topShape=v-m
- C5: counts=2,1,1,1, ¬Lx, ¬Rx, xTop=right, topShape=v-m
- C6: rhsVars=3, xTop=right, topShape=v-m, x appears exactly twice
- C7: rhsVars=3, ¬Lx, ¬Rx, xTop=left, topShape=m-v, x appears exactly twice
- C8: rhsVars=3, Lx, xTop=left, topShape=m-v
- C9: counts=2,2,1, Lx, ¬Rx, xTop=both, topShape=v-m
- C10: counts=2,2,1, ¬Lx, Rx, x appears exactly twice
- C11: counts=3,1,1, ¬Lx, ¬Rx, xTop=right, topShape=v-m
- C12: counts=3,1,1, ¬Lx, ¬Rx, xTop=left, topShape=m-v
- C13: counts=2,1,1,1, ¬Lx, Rx
- C14: B is bare, counts=3,1,1, Rx, ¬square

### STEP 1 — Compute features for both A and B

- `size` = total var occurrences on both sides
- `vars` = number of distinct variables
- `imb` = |left_x_count − right_x_count| summed over all variables
- `bare` = one side is a single variable
- `LP` = leftmost vars match across the two sides
- `RP` = rightmost vars match
- `SET` = same variable set both sides
- `XOR` = every variable has matching parity
- `AB` = every variable has exactly matching counts

### STEP 2 — Separator tests (one hit ⇒ FALSE)

If A has any of LP, RP, SET, XOR, AB, but B does NOT have the same property → FALSE.

### STEP 3 — Guards (any hit ⇒ FALSE)

- G1: bare(A), ¬bare(B), size(A)=size(B), vars(B)<vars(A), imb(B)>imb(A), XOR(A)
- G2: bare(A), vars(A)=2, ¬bare(B), vars(B)>vars(A), size(B)≥size(A)
- G3: ¬bare(A), vars(A)≥5, ¬bare(B), vars(B)<vars(A), size(B)=size(A)
- G4: ¬bare(A), ¬bare(B), size(B)=size(A), vars(B)≤vars(A)−2

### STEP 4 — Structural rules (first match wins)

Let sA, sB = sizes; vA, vB = vars; iA, iB = imbalances.

- T4: bare(A), vA≥4, vB=2 → TRUE
- T6: bare(A), ¬RP(A), XOR(A), vB=2, RP(B) → TRUE
- T7: ¬bare(A), vA=3, iA=2, vB=3, ¬XOR(B) → TRUE
- T8: vA=3, ¬XOR(A), bare(B), ¬RP(B) → TRUE
- F1: vB=vA → FALSE
- F2: iB<iA → FALSE
- F3: sB<sA → FALSE
- T1: bare(A), vA≥3, sB>sA → TRUE
- T2: bare(A), bare(B), vA≥3, vB>vA, iB>iA → TRUE
- T5: vB<vA, iB>iA → TRUE

### STEP 5 — Default

If nothing above fired: check D3 = every variable in A has uniform total count across both sides AND vB<vA AND B is a bare law of form `x = x*(…)` → TRUE.

Otherwise FALSE.

## CRITICAL: Avoid defaulting to FALSE without systematic check

Before outputting FALSE, verify you actually:
1. Normalized A correctly and checked all STEP 0 projections
2. Computed the features correctly if A is bare (STEP 0B)
3. Walked through STEP 4 in order

If rules are ambiguous or you're unsure, try one size-2 magma (e.g., left projection `p*q=p`, right projection, xor) as a quick sanity check. If A holds in that magma and B fails, FALSE. If you can't falsify in any simple magma, the default is TRUE (not FALSE).

## Output format — mandatory

Write at most 400 words of reasoning naming which rule fired. Then end with:

VERDICT: TRUE
\boxed{TRUE}

OR

VERDICT: FALSE
\boxed{FALSE}

The `\boxed{...}` line MUST be the very last line. If you run out of space, stop and emit the boxed verdict NOW.
